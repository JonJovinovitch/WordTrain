node_modules/
# build output only — the leading slash keeps this from also matching
# client/public/, which holds real source assets like the theme mp3
/public/
.env
.local-scores.json
*.log
.DS_Store
